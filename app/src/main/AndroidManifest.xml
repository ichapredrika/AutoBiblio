<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.predrika.icha.autobiblio">
    //camera permission
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_foreground"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegistrationActivity" />
        <activity android:name=".ProfileSettingActivity" />
        <activity android:name=".Scanner" />
        <activity android:name=".GeneratorActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".CollectionActivity" />
        <activity android:name=".HistoryActivity" />
        <activity android:name=".ScannerActivity" />
        <activity android:name=".CollectionViewActivity" />
        <activity android:name=".CollectionSearchActivity" />
        <activity android:name=".ReturnScanner" />
        <activity android:name=".TopUpScanner" />
        <activity android:name=".PayFinesActivity" />
        <activity android:name=".HistoryAdminActivity" />
        <activity android:name=".BorrowingActivity" />
        <activity android:name=".ReturningActivity" />
        <activity android:name=".ReportingActivity"></activity>
    </application>

</manifest>